"""Drive sync removed — Google Drive dependency dropped."""


def append_questions_to_prep_doc(company_name, job_title, questions):
    """Formerly synced to Google Drive. Now a no-op — questions are stored in SQLite."""
    return False
